export interface StatCard {
    date: string,
    discipline:  string,
    measurement: string,
    unit: string,
    currentDistance: number,
    currentHours: number,
    currentMinutes: number,
    currentSeconds: number
}
